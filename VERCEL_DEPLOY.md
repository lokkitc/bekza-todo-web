# Деплой фронтенда в Vercel

## 🚀 Быстрый деплой

### Вариант 1: Через Vercel Dashboard

1. Зайдите на [vercel.com](https://vercel.com)
2. Подключите ваш GitHub/GitLab репозиторий
3. Настройте проект:
   - **Root Directory**: `web`
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`

4. Добавьте переменные окружения:
   - `VITE_API_BASE_URL` - URL вашего API (например: `https://your-api-domain.com/api/v1`)

5. Нажмите **Deploy**

### Вариант 2: Через Vercel CLI

```bash
cd web
npm install -g vercel
vercel login
vercel
```

Для production деплоя:
```bash
vercel --prod
```

### Переменные окружения

В настройках проекта Vercel добавьте:

- **VITE_API_BASE_URL** - URL вашего API бэкенда
  - Пример: `https://bekza-todo-api-866428924028.us-central1.run.app/api/v1`
  - Не должен заканчиваться на `/`

### Автоматический деплой

После настройки, каждый push в `main` ветку автоматически деплоит проект.

### Локальная разработка

```bash
cd web
npm install
npm run dev
```

Для локальной разработки API URL берется из `.env.local` или используется значение по умолчанию.

### Структура конфигурации

- `vercel.json` - конфигурация деплоя Vercel
- `.vercelignore` - файлы, которые игнорируются при деплое
- `src/shared/config/env.ts` - конфигурация API URL

### Настройка домена

1. В Vercel Dashboard перейдите в Settings > Domains
2. Добавьте ваш домен
3. Настройте DNS записи согласно инструкциям Vercel

### Troubleshooting

#### Сборка не проходит
- Убедитесь, что `package.json` содержит все зависимости
- Проверьте, что TypeScript компилируется без ошибок: `npm run build`

#### API запросы не работают
- Проверьте переменную окружения `VITE_API_BASE_URL` в Vercel
- Убедитесь, что API доступен и CORS настроен правильно
- Проверьте CORS настройки в `app.yaml` на бэкенде

#### 404 ошибки при переходах по страницам
- Проверьте настройку rewrites в `vercel.json`
- Убедитесь, что используется client-side routing

